# Generated by go2rpm 1.6.0
%bcond_with check
%bcond_without bootstrap
%global debug_package %{nil}

# https://github.com/GoogleCloudPlatform/google-cloud-go
%global goipath         cloud.google.com/go/logging
%global forgeurl        https://github.com/GoogleCloudPlatform/google-cloud-go
Version:                1.8.1
%global tag             logging/v1.8.1
%global distprefix      %{nil}

%gometa

%global common_description %{expand:
Go packages for Google Cloud Platform services.}


%global golicenses      LICENSE
%global godocs          CHANGES.md CODE_OF_CONDUCT.md CONTRIBUTING.md\\\
                        README.md RELEASING.md SECURITY.md testing.md\\\

Name:           %{goname}
Release:        %autorelease
Summary:        Google Cloud Client Libraries for Go

License:        BSD-3-Clause AND Apache-2.0
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
%goprep
# remove all modules but logging
find %{_builddir}/google-cloud-go-logging-v%{version}/* -depth -type d -not -path '*/logging*' -not -path '*/_build*' -exec rm -rf {} \;
rm doc.go
mv logging/* .

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
